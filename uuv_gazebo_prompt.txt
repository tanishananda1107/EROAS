Convert ROS1 Gazebo stack to ROS2 Jazzy.

WORKDIR:
uuv_simulator/uuv_gazebo

RULES:
ONLY output file contents.

NO explanations.
NO markdown.
NO text.

REPLACE:
gazebo_ros → ros_gz_sim
gazebo_ros_control → gz_ros2_control
tf → tf2_ros

CMAKE:
must use ament_cmake only

package.xml:
format="3"
remove catkin completely

LAUNCH:
convert XML launch → Python launch files (launch.py)

URDF:
replace gazebo plugins with gz_ros2_control compatible plugins
