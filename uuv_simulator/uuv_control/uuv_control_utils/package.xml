
<?xml version="1.0"?>
<package format="2">
  <name>uuv_control_utils</name>
  <version>0.6.13</version>
  <description>ROS2 utilities for UUV control</description>

  <maintainer email="musa.marcusso@de.bosch.com">
    Musa Morena Marcusso Manhaes
  </maintainer>

  <license>Apache-2.0</license>

  <build_depend>ament_cmake</build_depend>
  <depend>rclpy</depend>

  <!-- Messages -->
  <depend>std_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>visualization_msgs</depend>

  <!-- Gazebo -->
  <depend>gazebo_msgs</depend>

  <!-- UUV -->
  <depend>uuv_control_msgs</depend>
  <depend>uuv_world_ros_plugins_msgs</depend>
  <depend>uuv_gazebo_ros_plugins_msgs</depend>

  <!-- Python dependencies -->
  <exec_depend>python3-numpy</exec_depend>
  <exec_depend>python3-yaml</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>

And here are the converted code snippets for C++ and Python:

C++:
#include <rclpy/rclpy.hpp>
#include <tf2_ros/tf2_ros.h>

// ...

void my_node_main(int argc, char** argv) {
    // ...
    tf::StampedTransform transform = tf2::fromMsg(transform_msg);
    // ...
}

Python:
import rclpy
from tf2_ros import Buffer, fromMsg

class MyNode(rclpy.Node):
    def __init__(self):
        super().__init__('my_node')
        self.create_publisher()
        self.create_subscription()

    def my_method(self):
        publisher = self.create_publisher('my_topic')

        subscription = self.create_subscription('my_topic', 10)

        service = self.create_service('my_service')

        timer = self.get_clock().now()

Please note that these are just examples and may require additional modific[7D[K
modifications to work correctly in your specific use case.

