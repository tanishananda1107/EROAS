Convert entire ROS1 control stack to ROS2 Jazzy.

WORKDIR:
uuv_simulator/uuv_control

PACKAGES:
uuv_trajectory_control
uuv_thruster_manager
uuv_control_utils
uuv_control_msgs
uuv_auv_control_allocator
uuv_control_cascaded_pids

RULES:
ONLY output raw file contents.

NO explanations.
NO markdown.
NO comments.
NO descriptions.
NO text.

HARD CONVERSIONS:
catkin → ament_cmake
rospy → rclpy
roscpp → rclcpp
tf → tf2_ros

CMAKE RULES:
must be valid ROS2 CMake only
NO catkin allowed

PACKAGE.XML RULES:
format="3"
remove all ROS1 dependencies
use ament_cmake only

FAIL SAFE:
If package is unclear, output minimal valid ROS2 package structure
