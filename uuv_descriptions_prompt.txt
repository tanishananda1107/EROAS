Convert ROS1 package to ROS2 Jazzy.

WORKDIR:
uuv_simulator/uuv_descriptions

RULES:
ONLY output file contents.

NO explanations.
NO markdown.
NO text.

CMakeLists.txt:
cmake_minimum_required(VERSION 3.8)
project(uuv_descriptions)
find_package(ament_cmake REQUIRED)
install(DIRECTORY launch meshes robots urdf DESTINATION share/${PROJECT_NAME})
ament_package()

package.xml:
format="3"
remove catkin
use ament_cmake only
remove rospy dependencies if possible

scripts:
convert rospy → rclpy
use rclpy.node.Node

NO ROS1 references allowed in final output
